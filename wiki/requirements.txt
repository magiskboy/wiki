# Phụ thuộc build site (Python >= 3.13). Cài vào .venv ở gốc repo:
#   uv pip install --python ../.venv/bin/python -r requirements.txt
#
# pyssg là static site generator (https://pyssg.nkthanh.dev), chỉ cài qua git.
# Pin v0.2.0: core nâng cấp (RSS có guid/pubDate, i18n-aware built-ins...) nhưng
# wiki vẫn dùng plugin riêng (codehilite, đồ thị tri thức, category theo thư mục,
# dashboard) vì đây là tính năng bespoke, không phải workaround cấu hình thiếu.
pyssg @ git+https://github.com/magiskboy/pyssg@v0.2.0
# pymdownx.arithmatex: bảo vệ công thức LaTeX để KaTeX render client-side
# (WikiMarkdown dùng); pyssg lõi không kéo theo gói này.
pymdown-extensions>=10
