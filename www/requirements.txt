# Phụ thuộc build site (Python >= 3.13). Cài vào .venv:
#   uv venv && uv pip install -r requirements.txt
#
# Cogito Publish hiện vẫn dùng import namespace `pyssg`, nhưng CLI được đổi sang
# `cogito-publish`. Pin trực tiếp vào wheel phát hành để local/CI dùng cùng bản.
pyssg @ https://github.com/CogitoForge-AI/cogito-publish/releases/download/v0.1.0/pyssg-0.1.0-py3-none-any.whl
# pymdownx.arithmatex: bảo vệ công thức LaTeX để KaTeX render client-side;
# truyền vào markdown(extensions=[...]). pyssg lõi không kéo theo gói này.
pymdown-extensions>=10
